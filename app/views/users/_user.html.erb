<li>
  <%= gravatar_for user, size: Settings.valid.gravata %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.current_user?(current_user) %>
    <%= t "or" %> <%= link_to t("delete"), user, method: :delete,
      data: {confirm: t("you_sure")} %>
  <% end %>
</li>
